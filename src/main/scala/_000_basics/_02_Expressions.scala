package _000_basics

/**
 * Expression: "code which evaluates when a program is run to produce a single value."
 * ...
 * "Expressions are the most fundamental building-blocks of Scala code."
 * ...
 * Everything in Scala is an Expression. Only definitions such as val or class or package are not expressions,
 * everything else is. 
 * ***
 * Read more: https://scala.zone/courses/scala/beginner/foundations/expressions
 */
object _02_Expressions extends App {
  println(4 + 9)  // 4 + 9 is an expression which produce 13 as a value
}
